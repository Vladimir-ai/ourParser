int main()
{
    int a[5];
    for(int i = 0; i < 5; i= i +1);
        a[i] = i;
    int b = 0 - 4;
    b = -2;
    float t = 1e-7;
    int f = 5;
    while(f)
    {
        a[f-1] = a[f-1] + b;
        f = f -1 ;
    }

    for(;;);
    for(;;){}
    while(1);
    while(1){}
    if(1);
    if(1){}
    ;;
    func(a[a[i + 6] + b[0]]);
    7;;;;
    p;
    return func(i + 1);
    return 0;
    return -1;
    return;
    d = add[i] + g[g[0] + 1] / 1;
    if(g == 0);
    if(g < 0);
    if(g > 0);
    if(g <= 0);
    if(g >= 0);
    if(g > 0 && g == 0);
    for(int b = 0 ;; b = b + 1);
    for(;g < 0; g = g -1);
    a[0] = 0;
    ar[0] = -1;
    void d(){}
    void d(int a, int b){}
    charC d(int a[1]){} //zdes umer odin homyach'eck
    a = -1;
    int d(int a[10]){} //zdes umer vtoroi homyach'eck
    d(a[10]);
    2==2;
    int[] func(){
        int a = -5;
    }
    aaaa func(){
    }
    b = g==g;
    b = 0x10;
   // b = 010; //zdes umer NaN homyach'eck ¯\_(ツ)_/¯
    b = 0o10;
    b = 0b11;
    ;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
}